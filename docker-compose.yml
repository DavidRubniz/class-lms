version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # הגדרת "ווליומים" כדי שהנתונים יישמרו מחוץ לקונטיינר
      - ./db.sqlite3:/app/db.sqlite3
      - ./media:/app/media
    restart: unless-stopped